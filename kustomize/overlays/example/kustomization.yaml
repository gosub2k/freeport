# This is the not a generic example: It's built for a
# specific microk8s cluster with specific USB devices already plugged in/

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: freeport

resources:
  - ../../base
  - storageclasses.yaml
